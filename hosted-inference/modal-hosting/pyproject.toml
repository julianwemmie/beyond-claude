[project]
name = "modal-hosting"
version = "0.1.0"
description = "Three-way inference comparison: Ollama vs Modal (vLLM) vs Groq"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "modal>=1.3.5",
    "openai>=1.0",
]
